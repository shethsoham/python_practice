'''
Write a Program to check whether the Character is Uppercase or
Lowercase Character.
Input: v
Output: letter v is in lowercase
'''

char = input( "enter the character you want" )

if char > 'a' and char <'z' :
    print(char,"it is lower case ")

elif char >'A' and char < 'Z':
    print (char, "it is a UPPER case alphabate")

else :
    print ("the thing which you entered is not an alphabate") 